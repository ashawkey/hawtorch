{
    "model": "MNISTClassifier",
    "optimizer": "Adam",
    "objective": "CrossEntropyLoss",
    "workspace_path": "./workspace/mnist",
    "device": "cuda",
    "epochs": 10,
    "train_batch_size": 32,
    "val_batch_size": 32,
    "test_batch_size": 32,
    "lr": 0.001,
    "lr_decay": 0.1,
    "lr_decay_step": 10,
    "weight_decay": 0
}